export enum EnOrderStatus {
  Registration = 1,
  Production = 2
}

export enum OrderStatus {
  ordRegistration = 1,
  ordProduction = 2,
  ordArchived = 3,
  reclInbox = 10,
  reclDecision = 11,
  reclProduction = 12,
  reclArchived = 13
}

export enum Roles {
  general = 1,
  management = 2,
  bookkeeping = 4,
  manufacture = 3
}
